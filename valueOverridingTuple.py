# Dimensions (200,50)
dimensions = (200,50)

#Printing the dimensions and have to cast to a string as cannot print tuple as a string
print("Original dimensions : " + str(dimensions))

#Changing the values 
dimensions = (400, 100)
print("\nModified values : " + str(dimensions))